--executar ambientes de debug por CLI

dotnet run --environment "Production"(NOME QUE ESTÁ NO LAUNCHSETTINGS.JSON) //Força as variáveis de ambiente de produção.

EXECUTAR UM PERFIL EXCLUSIVO NA MÃO
dotnet run --launch-profile "production" //só funciona se rodar no kestrel

--para saber em qual ambiente está rodando